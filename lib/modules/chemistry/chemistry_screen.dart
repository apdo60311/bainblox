import 'package:flutter/material.dart';

class ChemistryScreen extends StatelessWidget {
  static const String routeName = "Chemistry";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Chemistry"),
      ),
    );
  }
}
