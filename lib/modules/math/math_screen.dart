import 'package:flutter/material.dart';

class MathScreen extends StatelessWidget {
  static const String routeName = "Math";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Math"),
      ),
    );
  }
}
