import 'package:flutter/material.dart';

class EnglishScreen extends StatelessWidget {
  static const String routeName = "English";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("English"),
      ),
    );
  }
}
