import 'package:flutter/material.dart';

class ArabicScreen extends StatelessWidget {
  static const String routeName = "Arabic";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Arabic"),
      ),
    );
  }
}
